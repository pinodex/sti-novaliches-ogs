<?php

/*
 * This file is part of the Boardy forum software
 *
 * (c) Raphael Marco <raphaelmarco@outlook.com>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */

/*
 * Debug mode
 *
 * Shows debug, error, and exception information.
 */
$app['debug'] = false;

/*
 * Application cache directory
 */
$app['application.cache_dir'] = sys_get_temp_dir() . '/snogs-cache/application';

/*
 * Profiler options
 */
$app['profiler.cache_dir'] = sys_get_temp_dir() . '/snogs-cache/profiler';

/*
 * Twig options
 */
$app['twig.options'] = array(
    'cache' => sys_get_temp_dir() . '/snogs-cache/twig'
);

/*
 * Database connection
 *
 * Defines the connection parameters that will be used to
 * connect to your database.
 */
$app['database.connection'] = array(
    'driver'    => 'mysql',
    'host'      => 'localhost',
    'database'  => '',
    'username'  => '',
    'password'  => '',
    'charset'   => 'utf8',
    'collation' => 'utf8_unicode_ci'
);

/*
 * Session storage options
 *
 * Refer to http://silex.sensiolabs.org/doc/providers/session.html#parameters
 * for available options.
 */
$app['session.storage.options'] = array(
    'name'              => 'session',
    'cookie_httponly'   => true,
    'cookie_lifetime'   => 3600,
    'lifetime'          => 3600
);
